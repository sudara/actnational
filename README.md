# Act National

Local setup just uses sqlite.


## bundle

`bundle install`

## Setup .env

You'll need a `.env` file to stick secrets in. Do this:

`cp .sample.env .env`


## Seed data

`rails db:seed`
